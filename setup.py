from setuptools import setup


setup(
    name='flask-hcv-example',
    version='1.0',
    install_requires=[
        'flask'
    ],
    packages=[
        'example'
    ],
    entry_points={
        'console_scripts': [
        ]
    }
)
